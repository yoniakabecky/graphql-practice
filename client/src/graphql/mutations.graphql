input BookInput {
  name: String!
  genre: String!
  authorId: ID!
}

mutation AddBook($data: BookInput!) {
  addBook(data: $data)
}

input AuthorInput {
  name: String!
  age: String!
  authorId: ID!
}

mutation AddAuthor($data: AuthorInput!) {
  addAuthor(data: $data)
}
